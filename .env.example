# 複製成 .env（或直接在服務設定檔/工作排程器裡設同名環境變數），依部署機器調整。
# app.py / wsgi.py 三個變數都有預設值，這個檔案只在需要覆寫時才需要。

# 服務監聽的埠號
PORT=5000

# 送單紀錄（json + docx）存放位置，預設是 repo 底下的 archive/
# 想存到別的磁碟/資料夾（例如有另外備份機制的磁碟）時才需要設
# ARCHIVE_DIR=D:\dc4018-data\archive

# 識別資料 SQLite 檔位置，預設是 repo 底下的 data/identities.db
# DB_PATH=D:\dc4018-data\identities.db
